<Window x:Class="OlympusServiceBusApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OlympusServiceBusApplication"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Olympus Service Bus Configurator" Height="250" Width="700">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Contracts Root Directory"
                   FontSize="16"
                   FontWeight="SemiBold" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="140" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding ContractsRootDirectory, UpdateSourceTrigger=PropertyChanged}"
                     MinHeight="32"
                     VerticalContentAlignment="Center" />

            <Button Grid.Column="2"
                    Content="Browse..."
                    MinHeight="32"
                    Command="{Binding BrowseContractsRootDirectoryCommand}" />
        </Grid>
    </Grid>
</Window>
